# prisma
prisma/generated

# per-network Predict deployments. testnet stays committed as deployed.json; localnet and
# other ephemeral chains (ids change on every regenesis) are never committed.
src/lib/sui/deployed.*.json

# localnet test-publish ephemeral publication file (regenerated every bootstrap)
Pub.*.toml
